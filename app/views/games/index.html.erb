<div class="page-header">
	<h1>View Games</h1>
</div>

<% if Game.all.count < 1 %>
  <h4>No games yet! Be the first to <%= link_to "create one", new_game_path %>.</h4>
<% else %>
<%= render @games%>
<%= paginate @games %>
<% end %>
