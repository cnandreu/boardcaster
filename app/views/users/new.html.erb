<h1>Sign Up</h1> 
 
<p>If you already have an account, <%= link_to "Login Here", login_path %>.</p>

<%= render 'shared/error_messages', :target =>  @user %>

<%= render 'users/edit_fields', :send_text => "Submit" %>

